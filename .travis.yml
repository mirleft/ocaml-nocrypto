language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
env:
  - PACKAGE="nocrypto" OCAML_VERSION=4.02
  - PACKAGE="nocrypto" OCAML_VERSION=4.02 DEPOPTS=lwt
  - PACKAGE="nocrypto" UPDATE_GCC_BINUTILS=1 OCAML_VERSION=4.02 DEPOPTS=mirage-xen TESTS=false
  - PACKAGE="nocrypto" OCAML_VERSION=4.03
  - PACKAGE="nocrypto" OCAML_VERSION=4.03 DEPOPTS=lwt
  - PACKAGE="nocrypto" UPDATE_GCC_BINUTILS=1 OCAML_VERSION=4.03 DEPOPTS=mirage-xen TESTS=false
notifications:
  email: false
